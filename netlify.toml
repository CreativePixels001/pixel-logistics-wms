[build]
  # Directory where the build is run
  base = "frontend"
  
  # No build command needed (static site)
  command = ""
  
  # Directory to publish (must be relative to base)
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# Redirect root to landing page
[[redirects]]
  from = "/"
  to = "/landing.html"
  status = 200
  force = true

# Redirect all other requests to landing page for SPA routing
[[redirects]]
  from = "/*"
  to = "/landing.html"
  status = 200
  force = false

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    
[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    
[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
